# Tech Stack

> Technology stack documentation for {{project.name}}

## Overview

| Category | Technology | Version |
|----------|------------|---------|
| Language | {{capitalize project.language}} | {{#if (eq project.language "typescript")}}5.x{{else}}-{{/if}} |
{{#if project.framework}}
| Framework | {{project.framework}} | {{#if project.version}}{{project.version}}{{else}}latest{{/if}} |
{{/if}}
{{#if detection.buildTool}}
| Build Tool | {{detection.buildTool}} | latest |
{{/if}}
{{#if detection.packageManager}}
| Package Manager | {{detection.packageManager}} | latest |
{{/if}}
{{#if detection.testing}}
| Testing | {{detection.testing}} | latest |
{{/if}}
{{#if detection.styling}}
| Styling | {{detection.styling}} | latest |
{{/if}}
{{#if detection.database}}
| Database | {{detection.database}} | latest |
{{/if}}

## Core Technologies

{{#if (eq project.language "typescript")}}
### TypeScript

TypeScript is the primary language, providing static typing and enhanced developer experience.

**Configuration**: `tsconfig.json`

Key settings:
- Strict mode enabled
- ES modules
- Path aliases configured
{{/if}}

{{#if (eq project.language "javascript")}}
### JavaScript

Modern JavaScript (ES2022+) with optional JSDoc type annotations.
{{/if}}

{{#if (eq project.language "python")}}
### Python

Python 3.10+ with type hints and modern async patterns.

**Configuration**: `pyproject.toml`
{{/if}}

{{#each detection.frameworks}}
### {{name}}

{{#is name "React"}}
React for building user interfaces with component-based architecture.

Key features used:
- Functional components with hooks
- Context API for state management
- Suspense for data fetching
{{/is}}

{{#is name "Next.js"}}
Next.js for full-stack React development with server-side rendering.

Key features used:
- App Router
- Server Components
- API Routes
- Image Optimization
{{/is}}

{{#is name "Vue"}}
Vue 3 for building reactive user interfaces.

Key features used:
- Composition API
- Script setup syntax
- Pinia for state management
{{/is}}

{{#is name "Express"}}
Express.js for building RESTful APIs.

Key features used:
- Middleware pattern
- Router for modular routes
- Error handling middleware
{{/is}}

{{#is name "FastAPI"}}
FastAPI for building high-performance Python APIs.

Key features used:
- Pydantic for validation
- Async/await patterns
- OpenAPI documentation
{{/is}}
{{/each}}

## Development Tools

{{#if detection.buildTool}}
### {{detection.buildTool}}

{{#is detection.buildTool "vite"}}
Vite for fast development server and optimized production builds.
{{/is}}
{{#is detection.buildTool "webpack"}}
Webpack for module bundling and asset optimization.
{{/is}}
{{#is detection.buildTool "esbuild"}}
esbuild for ultra-fast TypeScript/JavaScript bundling.
{{/is}}
{{/if}}

{{#if detection.testing}}
### Testing: {{detection.testing}}

{{#is detection.testing "jest"}}
Jest for unit and integration testing with excellent mocking capabilities.
{{/is}}
{{#is detection.testing "vitest"}}
Vitest for fast, Vite-native testing.
{{/is}}
{{#is detection.testing "bun"}}
Bun's built-in test runner for fast testing.
{{/is}}
{{#is detection.testing "pytest"}}
pytest for Python testing with fixtures and plugins.
{{/is}}
{{/if}}

{{#if detection.styling}}
### Styling: {{detection.styling}}

{{#is detection.styling "tailwind"}}
Tailwind CSS for utility-first styling.
{{/is}}
{{#is detection.styling "styled-components"}}
Styled Components for CSS-in-JS.
{{/is}}
{{#is detection.styling "css-modules"}}
CSS Modules for scoped styling.
{{/is}}
{{/if}}

## Infrastructure

{{#if detection.database}}
### Database: {{detection.database}}

{{#is detection.database "postgresql"}}
PostgreSQL for reliable relational data storage.
{{/is}}
{{#is detection.database "mongodb"}}
MongoDB for flexible document storage.
{{/is}}
{{#is detection.database "mysql"}}
MySQL for relational data storage.
{{/is}}
{{/if}}

### Package Manager: {{detection.packageManager}}

{{#is detection.packageManager "bun"}}
Bun for fast package management and runtime.
{{/is}}
{{#is detection.packageManager "pnpm"}}
pnpm for efficient disk-space usage with hard links.
{{/is}}
{{#is detection.packageManager "npm"}}
npm as the standard Node.js package manager.
{{/is}}
{{#is detection.packageManager "yarn"}}
Yarn for reliable dependency management.
{{/is}}

## Version Requirements

| Technology | Minimum Version |
|------------|-----------------|
{{#if (or (eq project.language "typescript") (eq project.language "javascript"))}}
| Node.js | 18.x |
{{/if}}
{{#if (eq project.language "python")}}
| Python | 3.10 |
{{/if}}
{{#if (eq project.language "typescript")}}
| TypeScript | 5.0 |
{{/if}}
{{#if detection.packageManager}}
| {{detection.packageManager}} | latest |
{{/if}}

---
*Generated by AIde v{{generated.version}}*
