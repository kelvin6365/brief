---
trigger: manual
description: Technology stack and project configuration
---
<!-- 
  Qoder Rule Configuration:
  - This rule provides project context for Qoder's memory system
  - Recommended: "Always Apply" or "Model Decision"
  - Qoder's Automatic Memory will learn from this content
  See: https://docs.qoder.com/user-guide/chat/memory
-->

# Project Information: {{project.name}}

> Technology stack and project configuration reference

**Note:** This file feeds Qoder's **Automatic Memory** system, which learns your project's tech stack, coding style, and patterns. Qoder will reference this context automatically across sessions.

## Project Overview

- **Project Name**: {{project.name}}
- **Project Type**: {{project.type}}
- **Primary Language**: {{project.language}}
- **Generated**: {{generated.timestamp}}
- **Brief Version**: {{generated.version}}

---

## Technology Stack

**Language**: {{project.language}}

{{#if detection.frameworks}}
**Frameworks:**
{{#each detection.frameworks}}
- **{{this.name}}**{{#if this.description}} - {{this.description}}{{/if}}
{{/each}}
{{/if}}

{{#if detection.packageManager.name}}
**Package Manager**: {{detection.packageManager.name}}
{{/if}}

{{#if detection.runtime}}
**Runtime**: {{detection.runtime}}
{{/if}}

---

## Testing & Quality

{{#if detection.testing}}
**Testing Frameworks:**
{{#each detection.testing}}
- **{{this.name}}**
{{/each}}
{{else}}
**Testing**: Not yet configured
- **Recommendation**: Set up testing framework (see `@testing.md`)
{{/if}}

{{#if detection.linter}}
**Code Quality Tools:**
{{#each detection.linter}}
- {{this.name}}
{{/each}}
{{/if}}

---

## Environment Configuration

### Environment Files

```bash
.env                 # Development (gitignored)
.env.local          # Local overrides (gitignored)
.env.example        # Template (committed to git)
```

---

## Project Structure

```
{{#if (contains detection.frameworks.[0].name "Next.js")}}
project/
├── app/                # Next.js App Router
│   ├── api/           # API routes
│   └── layout.tsx     # Root layout
├── components/        # React components
├── lib/              # Utilities
├── public/           # Static assets
└── package.json

{{else if (contains detection.frameworks.[0].name "React")}}
project/
├── src/
│   ├── components/   # React components
│   ├── hooks/        # Custom hooks
│   ├── utils/        # Utilities
│   └── App.tsx
├── public/           # Static assets
└── package.json

{{else if (contains detection.frameworks.[0].name "Express")}}
project/
├── src/
│   ├── routes/       # API routes
│   ├── controllers/  # Request handlers
│   ├── services/     # Business logic
│   ├── middleware/   # Express middleware
│   └── index.ts
└── package.json

{{else if (contains detection.frameworks.[0].name "FastAPI")}}
project/
├── app/
│   ├── routers/      # API routers
│   ├── models/       # Pydantic models
│   ├── services/     # Business logic
│   └── main.py
├── tests/
└── requirements.txt

{{else}}
project/
├── src/              # Source code
├── tests/            # Tests
└── package.json / requirements.txt
{{/if}}
```

---

## Related Rules

- `@core.md` - Core coding standards
- `@best-practices.md` - Project-specific best practices
- `@requirements-spec.md` - Feature specifications
- `@security.md` - Security guidelines
- `@testing.md` - Testing standards
- `@error-handling.md` - Error handling patterns
{{#if (includes project.type "api")}}
- `@api-design.md` - API design patterns
{{/if}}
- `@git-workflow.md` - Git conventions

---

*Generated by Brief v{{generated.version}} for Qoder IDE*
