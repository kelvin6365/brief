# {{project.name}}

{{#if project.description}}
> {{project.description}}
{{/if}}

## Quick Facts

- **Project Type**: {{capitalize project.type}}
{{#if project.framework}}
- **Primary Framework**: {{project.framework}}{{#if project.version}} v{{project.version}}{{/if}}
{{/if}}
- **Primary Language**: {{capitalize project.language}}
{{#if detection.packageManager}}
- **Package Manager**: {{detection.packageManager}}
{{/if}}
{{#if detection.testing}}
- **Testing Framework**: {{detection.testing}}
{{/if}}
{{#if detection.database}}
- **Database**: {{detection.database}}
{{/if}}

## Tech Stack

{{#if detection.frameworks}}
### Frameworks & Libraries
{{#each detection.frameworks}}
- **{{name}}**{{#if version}} v{{version}}{{/if}}{{#if (gt confidence 0.8)}} (primary){{/if}}
{{/each}}
{{/if}}

{{#if detection.buildTool}}
### Build Tools
- {{detection.buildTool}}
{{/if}}

{{#if detection.styling}}
### Styling
- {{detection.styling}}
{{/if}}

## Project Structure

```
{{project.name}}/
├── src/                    # Source code
{{#if (hasFramework detection "react")}}
│   ├── components/         # React components
│   ├── hooks/              # Custom hooks
{{/if}}
{{#if (hasFramework detection "next")}}
│   ├── app/                # Next.js App Router
{{/if}}
{{#if (or (hasFramework detection "express") (hasFramework detection "fastapi"))}}
│   ├── routes/             # API routes
│   ├── controllers/        # Route controllers
│   ├── services/           # Business logic
{{/if}}
│   ├── utils/              # Utility functions
│   └── types/              # Type definitions
├── tests/                  # Test files
{{#if (eq project.language "typescript")}}
├── tsconfig.json           # TypeScript config
{{/if}}
└── package.json            # Project manifest
```

## Development Commands

```bash
# Install dependencies
{{detection.packageManager}} install

# Development mode
{{detection.packageManager}} run dev

# Build for production
{{detection.packageManager}} run build

# Run tests
{{detection.packageManager}} test

# Type check
{{#if (eq project.language "typescript")}}
{{detection.packageManager}} run type-check
{{/if}}
```

## Code Standards

### General Guidelines

1. **Follow existing patterns** - Look at similar code in the codebase first
2. **Keep it simple** - Prefer clear, readable code over clever solutions
3. **Test your changes** - Ensure code compiles and tests pass
4. **Small commits** - Make focused, atomic changes

{{#if (eq project.language "typescript")}}
### TypeScript

- Strict mode enabled - no `any` types
- Prefer interfaces for object shapes
- Use explicit return types for public functions
- Prefer `const` assertions for literal types
{{/if}}

{{#if (hasFramework detection "react")}}
### React

- Functional components with hooks
- Props interfaces for all components
- Use `useMemo`/`useCallback` for optimization
- Prefer composition over prop drilling
{{/if}}

## AI Assistant Notes

When working on this project:

- **Read before writing** - Always understand existing code first
- **Ask about architecture** - Confirm approach for significant changes
- **Validate changes** - Run type-check and tests
- **Follow conventions** - Match existing code style

{{#if config.templates}}
### Enabled Patterns
{{#each config.templates}}
- {{this}}
{{/each}}
{{/if}}

---
*Generated by AIde v{{generated.version}} on {{generated.date}}*
